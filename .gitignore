node_modules/
dist/
build/
.env
*.local
.DS_Store
backend/firebase-service-account.json

# Design sources — large binaries; keep design files outside the repo
*.psd
# Photoshop scratch files ("Untitled-N")
Untitled-*.psd

# User-uploaded content — runtime artifacts, not source
backend/uploads/

# Brand-asset inbox — source PNGs/SVG for the TapProve rebrand. The
# processed/ outputs are copied into the apps' src/assets and public/
# folders; the inbox itself stays local.
_inbox/
