/**
 * index.tsx — placeholder so expo-router has something to render. Replace
 * this with the real entry screen (likely a redirect to /login or /home
 * depending on auth state).
 */

import React from 'react';
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Dander — app skeleton ready. Add screens here.</Text>
    </View>
  );
}
