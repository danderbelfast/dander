/**
 * leaderboard.tsx — expo-router route for /leaderboard.
 *
 * The actual screen component lives in src/screens/Leaderboard.tsx so
 * it's reusable outside of this exact route path; this file is just the
 * file-based-routing entry point.
 */

export { default } from '../src/screens/Leaderboard';
