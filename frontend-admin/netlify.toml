[build]
  base      = "frontend-admin"
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  VITE_API_URL = "https://api.dander.io"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
